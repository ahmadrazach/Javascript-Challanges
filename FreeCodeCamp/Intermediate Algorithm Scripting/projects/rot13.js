function rot13(str) {
    //save all the uppercaseaplhabets in a string
    //make an accumulator
    //loop to the str
    // if found space or puntuation ? add it to accumulator
    // if it's a upper case letter ? check if +13 gets you to the number or subtract the number to take the result 
    return str;
  }
  
  rot13("SERR PBQR PNZC");